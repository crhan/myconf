fonts.conf：
    安装文泉驿微米黑之后在 http://bachue.is-programmer.com/posts/21850.html 找到的字体配置文件，显示效果非常不错
bashrc：
    基本的ubuntu的默认bashrc修改了一些选项。比如PS1，是从tx主席那里来的PS1
ubuntu.rc.local：
    放在/etc/rc.locaol的启动脚本文件，因为使用ssd，所以每次启动要修改文件调度方式为noop
vimrc & vim/：
    放在用户目录下即可
